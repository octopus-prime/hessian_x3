#
# what the heck...
#
branches:
  only:
  - master
dist: trusty
language: cpp
matrix:
  fast_finish: true
  include:
  - compiler: gcc
    env:
    install: sudo apt-get install -qq -y docker; docker pull ubuntu:16.10
    os: linux
    script: docker images
services:
- docker
sudo: required
